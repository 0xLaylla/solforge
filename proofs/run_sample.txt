```json
{
  "test_file": "// SPDX-License-Identifier: MIT\npragma solidity ^0.8.20;\n\nimport \"forge-std/Test.sol\";\nimport \"../src/SimpleToken.sol\";\n\ncontract SimpleTokenTest is Test {\n    SimpleToken token;\n    address owner;\n    uint256 constant INITIAL_SUPPLY = 1_000e18;\n\n    function setUp() public {\n        owner = address(this);\n        token = new SimpleToken(INITIAL_SUPPLY);\n    }\n\n    // === EDGE CASE TESTS (edge_case_hunter) ===\n\n    // edge_case: numeric extreme - zero amount transfer should succeed\n    function test_transferZeroAmount() public {\n        bool result = token.transfer(address(1), 0);\n        assertTrue(result);\n    }\n\n    // edge_case: address extreme - approve to zero address allowed per ERC20\n    function test_approveToZeroAddress() public {\n        token.approve(address(0), 100);\n        assertEq(token.allowance(address(this), address(0)), 100);\n    }\n\n    // === COVERAGE TESTS (coverage_analyzer) ===\n\n    // coverage: onlyOwner modifier happy path\n    function test_OwnerCanMint() public {\n        address recipient = address(0xBEEF);\n        vm.prank(owner);\n        token.mint(recipient, 100e18);\n        assertEq(token.balanceOf(recipient), 100e18);\n        assertEq(token.totalSupply(), INITIAL_SUPPLY + 100e18);\n    }\n\n    // coverage: onlyOwner modifier revert path\n    function test_RevertWhen_NonOwnerCallsMint() public {\n        address attacker = address(0xDEAD);\n        vm.prank(attacker);\n        vm.expectRevert(\"not owner\");\n        token.mint(attacker, 100e18);\n    }\n\n    // coverage: transfer happy path\n    function test_Transfer_Success() public {\n        address recipient = address(0xBEEF);\n        vm.prank(owner);\n        token.transfer(recipient, 50e18);\n        assertEq(token.balanceOf(recipient), 50e18);\n        assertEq(token.balanceOf(owner), INITIAL_SUPPLY - 50e18);\n    }\n\n    // coverage: transfer revert on zero address\n    function test_RevertWhen_TransferToZeroAddress() public {\n        vm.prank(owner);\n        vm.expectRevert(\"zero address\");\n        token.transfer(address(0), 50e18);\n    }\n\n    // coverage: transfer revert on insufficient balance\n    function test_RevertWhen_TransferInsufficientBalance() public {\n        address sender = address(0xCAFE);\n        vm.prank(sender);\n        vm.expectRevert(\"insufficient balance\");\n        token.transfer(owner, 1);\n    }\n\n    // coverage: approve happy path\n    function test_Approve_Success() public {\n        address spender = address(0xBEEF);\n        vm.prank(owner);\n        token.approve(spender, 100e18);\n        assertEq(token.allowance(owner, spender), 100e18);\n    }\n\n    // coverage: transferFrom happy path\n    function test_TransferFrom_Success() public {\n        address from = owner;\n        address to = address(0xBEEF);\n        address spender = address(0xCAFE);\n        vm.prank(from);\n        token.approve(spender, 100e18);\n        vm.prank(spender);\n        token.transferFrom(from, to, 60e18);\n        assertEq(token.balanceOf(to), 60e18);\n        assertEq(token.balanceOf(from), INITIAL_SUPPLY - 60e18);\n        assertEq(token.allowance(from, spender), 40e18);\n    }\n\n    // coverage: transferFrom revert on allowance exceeded\n    function test_RevertWhen_TransferFromAllowanceExceeded() public {\n        address from = owner;\n        address to = address(0xBEEF);\n        address spender = address(0xCAFE);\n        vm.prank(from);\n        token.approve(spender, 10e18);\n        vm.prank(spender);\n        vm.expectRevert(\"allowance exceeded\");\n        token.transferFrom(from, to, 50e18);\n    }\n\n    // coverage: transferFrom revert on insufficient balance\n    function test_RevertWhen_TransferFromInsufficientBalance() public {\n        address from = address(0xCAFE);\n        address to = address(0xBEEF);\n        address spender = address(0xDEAD);\n        vm.prank(from);\n        token.approve(spender, type(uint256).max);\n        vm.prank(spender);\n        vm.expectRevert(\"insufficient balance\");\n        token.transferFrom(from, to, 1);\n    }\n\n    // coverage: transferFrom revert on zero address\n    function test_RevertWhen_TransferFromToZeroAddress() public {\n        address from = owner;\n        address spender = address(0xCAFE);\n        vm.prank(from);\n        token.approve(spender, 50e18);\n        vm.prank(spender);\n        vm.expectRevert(\"zero address\");\n        token.transferFrom(from, address(0), 50e18);\n    }\n\n    // coverage: transferFrom exact allowance boundary\n    function test_TransferFrom_ExactAllowance() public {\n        address from = owner;\n        address to = address(0xBEEF);\n        address spender = address(0xCAFE);\n        vm.prank(from);\n        token.approve(spender, 50e18);\n        vm.prank(spender);\n        token.transferFrom(from, to, 50e18);\n        assertEq(token.allowance(from, spender), 0);\n    }\n}",
  "summary": {
    "total_tests": 13,
    "by_category": {
      "invariant": 0,
      "fuzz": 0,
      "edge": 2,
      "coverage": 11,
      "gas": 0
    }
  },
  "skipped": [
    {
      "agent": "edge_case_hunter",
      "name": "test_RevertWhen_transferToZeroAddress",
      "reason": "duplicate of coverage_analyzer test_RevertWhen_TransferToZeroAddress"
    }
  ]
}
```